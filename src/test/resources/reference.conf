co.rc.authmanager {
  persistence {
    authentication-db {
      driver = "slick.driver.H2Driver$"
      db {
        connectionPool = disabled
        driver = "org.h2.Driver"
        url = "jdbc:h2:mem:auth"
      }
    }
  }
}